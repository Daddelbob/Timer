import { RemainingSecondsPipe } from './remaining-seconds.pipe';

describe('RemainingSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
